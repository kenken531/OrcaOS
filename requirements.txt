textual>=0.52.0
psutil>=5.9.0
opencv-python>=4.8.0
numpy>=1.24.0
pyaudio>=0.2.13
# Volume control (Windows) — pure Python, works on 3.13
pycaw>=20230407
comtypes>=1.4.0
# MediaPipe — works on Python 3.13 (pure ctypes wheel, no ABI dependency)
# After installing, run: python download_model.py
mediapipe==0.10.35